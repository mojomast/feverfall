# FeverFall Font Attribution

Runtime text currently uses Bevy's default font handle for smoke scenes.  This
directory keeps the required Open Font License text so future checked-in fonts
can be validated before use.

No third-party font binaries are bundled in Checkpoint 5 render assets.
